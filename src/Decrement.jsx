import React from 'react';

const Decrement = ({contDecrement}) => {
    return (
        <div>
            <button onClick={contDecrement}>-</button>
        </div>
    );
}

export default Decrement;
